fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pyjwt==2.10.1
python-multipart==0.0.20
zeep==4.3.3
lxml==6.1.2
requests==2.34.2
python-dotenv==1.2.3
boto3==1.43.80
azure-storage-blob==12.30.0
bcrypt==4.2.1
apscheduler==3.11.0
# Optional AI/preview dependencies — install as needed:
# pdfplumber>=0.10  (PDF text extraction)
# python-docx>=1.1  (DOCX text extraction)
# openpyxl>=3.1     (XLSX text extraction)

# Optional IBM i / IBM Z connector dependencies — install as needed:
# paramiko>=3.4      (IBM i IFS access via SFTP)

# Optional IBM Watson AI dependencies — install as needed:
# ibm-watson>=8.1    (Watson NLU / Discovery SDK; alternatively use requests directly)
