fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pyjwt==2.10.1
python-multipart==0.0.20
zeep==4.3.3
lxml==6.1.2
requests==2.34.2
python-dotenv==1.2.3
boto3==1.43.80
azure-storage-blob==12.30.0
bcrypt==4.2.1
apscheduler==3.11.0
pdfplumber==0.11.10    # PDF text extraction for AI Q&A / classification / AI Agents knowledge bases
python-docx==1.2.0     # DOCX text extraction (same callers as above)
openpyxl==3.1.5        # XLSX text extraction (same callers as above)
bleach==6.4.0           # server-side allowlist HTML sanitizer for admin-authored rich-text content
Pillow==12.3.0          # verifies uploaded image files are genuine images (AI Agents pencil-editor image uploads)
python3-saml==1.16.0    # SAML SSO login (pulls in xmlsec, which has a prebuilt Windows wheel — verified working)
cryptography==50.0.1    # generates the SP's self-signed signing cert for SAML (already a transitive dep; pinned explicitly since it's imported directly now)
SQLAlchemy==2.0.36      # the app's own control-plane database engine (app/db.py) — sqlite by default, postgres/oracle when configured; already a transitive dep of something else, pinned explicitly since it's imported directly now

# Optional IBM i / IBM Z connector dependencies — install as needed:
# paramiko>=3.4      (IBM i IFS access via SFTP)

# Optional IBM Watson AI dependencies — install as needed:
# ibm-watson>=8.1    (Watson NLU / Discovery SDK; alternatively use requests directly)

# Optional app database drivers — install whichever FD_DB_ENGINE you set (see .env.example):
# psycopg[binary]>=3.2   (FD_DB_ENGINE=postgres)
# oracledb>=2.5          (FD_DB_ENGINE=oracle — pure-Python "thin" mode, no Oracle Client install needed)
