# IBM Bob task-session summaries

Each team member should export their IBM Bob task-session summary for this project and add it here (screenshot or exported file) before submitting — see the hackathon guide for the exact export steps.

Name each file `<team-member-name>-session.png` (or similar) so it's clear whose session it is.
